export interface VThemeColorOptions {
  fill?: boolean;
  color?: boolean;
  border?: boolean;
  background?: boolean;
}
export const defaultVThemeColorOptions: VThemeColorOptions = {
  background: true,
  border: true,
  color: true
};
